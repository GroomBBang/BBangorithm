SELECT FIRST_HALF.FLAVOR
FROM 
(
    SELECT FLAVOR, SUM(TOTAL_ORDER) AS JULY_ORDER
    FROM JULY
    GROUP BY JULY.FLAVOR
) AS SUB JOIN FIRST_HALF ON SUB.FLAVOR = FIRST_HALF.FLAVOR
ORDER BY ( JULY_ORDER + TOTAL_ORDER ) DESC
LIMIT 3
